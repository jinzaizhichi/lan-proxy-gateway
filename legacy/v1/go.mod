// This legacy directory is preserved for reference. It is a separate Go module
// so that `go build ./...` and `go test ./...` in the main project skip it.
// Do not import from here in the v2 code base.
module github.com/tght/lan-proxy-gateway/legacy/v1

go 1.25.0
